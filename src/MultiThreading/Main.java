package MultiThreading;

public class Main {
    public static void main(String[] args) {

        // multi threading = process of executing multiple threads simultaneously. Helps maximum utilizaiton of cpu.
        // threads are independent, they dont affect the execution of other threads.
        // an exception in one thred will not interrupt other threads.
        // useful for serving multiple clients
    }
}
